# Sample documents

Place plain-text (`.txt`) health or medical education files here.

Each file should be UTF-8 encoded. An optional first line `Title: ...` sets the display title used in source citations.

After adding or editing files, rebuild the index:

```bash
python main.py --build-index
```
